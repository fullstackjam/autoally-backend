replicaCount: 1

image:
  repository: harbor.fullstackjam.dev/app/autoally-backend
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  hosts:
    - host: autoally-backend.fullstackjam.dev
  tlsSecret: autoally-backend-tls-certificate

imagePullSecrets:
  name: my-harbor-secret
