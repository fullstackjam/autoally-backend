replicaCount: 3

image:
  repository: registry.fullstackjam.dev/autoally-backend
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  hosts:
    - host: autoally-backend.fullstackjam.dev
  tlsSecret: autoally-backend-tls-certificate
